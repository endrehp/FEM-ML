close all


for i=1:k
    
    cla
    clf
   
    p_d = predictions(i, :); %dependent predictions
    p_i = preds(i, :); %independent predictions
    y_t = y_test(i,:); %test data
    
    
    hold on
    plot(p_d, 'o')
    plot(p_i, )
    plot(f)
    %plot((unl-ul).^2)
    %disp(max((unl-ul).^2))
    axis([0 25 -0.03 0.03])
    
    drawnow
    pause(0.01)
    

end